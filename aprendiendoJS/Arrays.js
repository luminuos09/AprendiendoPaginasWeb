let nombres=[15,16,19]
//asi se añade elementos a una array
nombres.push(12)
//asi se elimina el ultimo elemento de una array
nombres.pop()
//asi podemos obtener la parte de una array, desde un punto hasta otro
algunosNombres= nombres.slice(0,2)
console.log(algunosNombres)
//asi podemos preguntar si este elemento existe
const categorias = ["comida", "transporte", "ocio"];
console.log(categorias.includes("comida"));
//asi podemos preguntar en que posicion estaba el elemento en la array
